export interface LocalUser {
    token: string;
    email: string;
    pref: string[];
    tpCamps: string[];
    ultimoLogin: string;
    perfis: number[];
}

export interface pwd {
    pwd: string;
}